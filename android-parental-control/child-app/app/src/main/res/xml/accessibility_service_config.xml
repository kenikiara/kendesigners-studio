<?xml version="1.0" encoding="utf-8"?>
<!-- Config for the FALLBACK app-block accessibility service (non-Device-Owner devices).
     It observes window-state changes to learn the foreground app; it does NOT read screen
     content. canRetrieveWindowContent is false to keep the footprint minimal. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:notificationTimeout="200"
    android:description="@string/transparency_body" />
